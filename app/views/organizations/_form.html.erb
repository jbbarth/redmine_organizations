<%= error_messages_for 'organization' %>

<!--[form:organization]-->
<div class="box tabular">
    <p><%= f.text_field :name %></p>
    <p><%= f.text_area :description, :size => "40x3" %></p>
    <p><%= f.select "parent_id", nested_set_options(Organization, @organizations) { |i|
                      "#{'--' * i.level} #{i.name}"
                    }, :include_blank => true %></p>
  </div>
<!--[eoform:organization]-->
