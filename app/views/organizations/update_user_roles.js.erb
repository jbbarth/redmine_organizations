$("#tab-content-members").html("<%= j render 'projects/settings/members' %>");
hideOnLoad();
initOrgasSelect2();
<% if @member %>
  $("#member-<%= @member.id %>").effect("highlight");
<% end %>

<% if @member && @member.errors.present? %>
  alert("<%= escape_javascript @member.errors.full_messages.join(', ') %>");
<% end %>
